<h1>Productos:</h1>
<% @products.each do |product| %>
<%= link_to product_path(product.id), class: 'product' do %>
<%= image_tag product.image, width: 100  if product.image.attached? %>
<h2><%= product.title %></h2>
<span><%= product.price %>$</span><br>
<% end %>
<% end %>